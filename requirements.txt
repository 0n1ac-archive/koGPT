# torch is preinstalled in the Vessl managed image (PyTorch 2.3.1 / CUDA 12.1).
# These are the extras our code needs on top of it.
datasets>=2.19
sentencepiece>=0.2.0
numpy>=1.24
huggingface_hub>=0.23
